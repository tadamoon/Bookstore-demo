<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
</head>
<body>
    <h1>Edit Book</h1>
    <form action="/books/<%=book._id%>?_method=PUT" method="POST">
        <label for="name">Book Name</label>
        <input type="text" id="name" name="name" placeholder="Book Name" value="<%=book.name%>">
        <label for="price">Book Price</label>
        <input type="number" id="price" name="price" placeholder="Book Price" value="<%=book.price%>" >
        <label for="author">Book Author</label>
        <input type="text" id="author" name="author" placeholder="Book Author" value="<%=book.author%>" >
        <label for="category">Category</label>
        <select name="category" id="category">
            <% for(let category of categories){ %>
            <option value="<%=category%>"<%=book.category===category ? 'selected' : ''%> ><%=category%></option>
            <% } %>
        </select>
        <button>Submit Changes!</button>
        </form>
        <a href="/books/<%=book._id%>">Cancel</a>
</body>
</html>